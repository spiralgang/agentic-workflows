name: repair-workflows

on:
  workflow_dispatch:
    inputs:
      target_repo:
        description: "owner/repo to repair (default: this repo)"
        required: false
        type: string
      model:
        description: "Model id (Mistral or OpenRouter)"
        required: false
        default: "mistral-large-latest"
        type: string

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout target repo
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.target_repo || github.repository }}
          token: ${{ secrets.GH_PAT }}
          path: target

      - name: Checkout scripts from agentic-workflows
        uses: actions/checkout@v4
        with:
          repository: spiralgang/agentic-workflows
          token: ${{ secrets.GH_PAT }}
          path: scripts
          sparse-checkout: .github/scripts

      - name: Set up Python
        if: steps.check_script.outputs.script_exists == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        working-directory: target
        run: pip install requests

      - name: Run Copilot-free repair agent (Mistral)
        if: steps.check_script.outputs.script_exists == 'true'
        env:
          MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}
          MISTRAL_MODEL: ${{ inputs.model }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          GH_PAT: ${{ secrets.GH_PAT }}
          REPO_OWNER: ${{ github.repository_owner }}
          TARGET_REPO: ${{ inputs.target_repo || github.repository }}
        working-directory: target
        run: python3 ../scripts/.github/scripts/fix_repo_workflows.py

      - name: Cleanup
        if: always()
        run: true