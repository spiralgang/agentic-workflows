# agentic-workflows

GitHub Agentic Workflows (`gh aw`) — aw-actions runtime, plus custom Termux
environment artifacts.

## Custom Termux env artifacts
- `spiralgang-custom-termux_usr.tar.gz` — custom `$PREFIX` (Termux /usr) snapshot
- `termux-custom-env_1.0.0_all.deb` — consolidated shell config + apkgen package
